# ralph-node

A Ralph Loop powered template repository for Node.js projects.

This is my Ralph loop. There are many like it, but this one is mine.

The initial PRD will establish the baseline configuration for Jest (unit testing), and Playwright (E2E testing), provide a working `npm test` command.